 
html
  head
    title Crear Categoría
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css")
  body
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
      .container-fluid
        a.navbar-brand(href='/categories') Categorías
        button.navbar-toggler(type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav")
          span.navbar-toggler-icon
        #navbarNav.collapse.navbar-collapse
          ul.navbar-nav
            li.nav-item
              a.nav-link(href='/categories') Lista de Categorías

    .container.mt-4
      h1 Crear Nueva Categoría
      form(action="/categories" method="POST" enctype="multipart/form-data")
        .mb-3
          label(for="name" class="form-label") Nombre
          input#name.form-control(type="text" name="name" required)
        .mb-3
          label(for="description") Descripción
          input#description.form-control(type="text" name="description" required)
        .mb-3
          label(for="image") Imagen URL
          input#image.form-control(type="file" name="image")
        button.btn.btn-primary(type="submit") Crear Categoría
